# Dependencies
node_modules
.pnp
.pnp.js

# Next.js
.next
out

# Production build
build
dist

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment files (.env.example is committed, everything else is not)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Netlify (build output written by the Next.js Runtime, and CLI site linkage)
.netlify

# Amplify
.amplify-hosting
amplify/\#current-cloud-backend
amplify/.config/local-*
amplify/backend/amplify-meta.json

# Testing
coverage

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE / Editor
.idea
.vscode
*.swp
*.swo
.DS_Store

# Build script caches (raw Overpass responses for the Finding us map)
.cache

# Source photography. Full resolution patient frames, kept out of the history on
# purpose: only the derived crops under public/images/ are needed to build the
# page. See assets/source/README.md.
assets/source/*
!assets/source/README.md

# Misc
*.pem
Thumbs.db
